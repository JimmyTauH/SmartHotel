main work

1、将权限界面进行细分

四类用户各自有其登录与功能界面
增加数据表：admin_recep（前台，复制了admin的数据表）
	    user_server（后勤，复制了user的数据表）
后端依照原有的格式进行增减，前台命名为RECEP（receptionist招待的缩写），后勤命名为SERVER（服务员嘛）
PS：原谅我靠谱的英语

前端为四类用户分别写了跳转路由路径，后期补充内容即可

这里有些问题，即前台与后勤会分别不断的select from admin和user

2、用户预约服务功能实现

为实现用户预约服务，利用原有note数据结构进行改进，数据及表示如下：
title：服务名称（设置了三类）、time：预约时间、content：备注  
并加了hotel表示服务所在酒店，这个得后期用户的入住酒店或房间进行修改

后端为前端设置了相关API接口，调用即可

前端在user的front中添加了ServiceBooking界面表示用户预约界面
目前能够预约（能够成功加入数据库）、展示（从数据库调取该用户所预约的服务）

后续需要加入后勤和管理员界面的服务预约展示。