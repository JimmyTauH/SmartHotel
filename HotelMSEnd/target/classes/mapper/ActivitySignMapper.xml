<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ActivitySignMapper">
    <sql id="Base_Column_List">
        id,activity_id,user_id,time,start_date,end_date
    </sql>
    <insert id="add">
        insert into activity_sigh
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="activity_id != null"> activity_id,</if>
            <if test="user_id != null"> user_id,</if>
            <if test="time != null"> time,</if>
            <if test="start_date != null"> start_date,</if>
            <if test="end_date != null"> end_date,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="activity_id != null"> #{activity_id},</if>
            <if test="user_id != null"> #{user_id},</if>
            <if test="time != null"> #{time},</if>
            <if test="start_date != null"> #{start_date},</if>
            <if test="end_date != null"> #{end_date},</if>
        </trim>
    </insert>

    <select id="selectAll" resultType="com.example.entity.ActivitySign">
        select activity_sign.*,activity.name as activityName,user.username from activity_sign
        left join activity
        on activity_sign.activity_id = activity.id
        left join user
        on activity_sign.user_id = user.id
        order by id desc
    </select>
</mapper>